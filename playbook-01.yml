---
- hosts: dev
  connection: local
  tasks:
    - name: Print message
      debug:
        msg: Hello Ansible World
    - name: copy file
      copy:
        src: /tmp/index.html
        dest: /tmp/odcdev/ansible-practice
